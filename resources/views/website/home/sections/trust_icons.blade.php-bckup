<div class="container max-w-container mx-auto">
    <div class="w-full grid gap-x-4 gap-y-4 grid-cols-1 md:grid-cols-3">

        @foreach ($data as $item)
            <div class="flex-box bg-pink-transparent/13 p-12 rounded-card">
                <img src="{{ asset($item['icon']) }}" class="max-w-icon max-w-(--max-w-xl) object-contain" alt="">
                <span class="text-block">{{ $item['text'] }}</span>
            </div>
        @endforeach

    </div>
</div>
